# Moats Verify — Configuration
# Copy to .env and fill in your values: cp .env.example .env

# =============================================================================
# REQUIRED — LLM Provider
# =============================================================================

# Provider: openrouter, ollama, openai, anthropic, custom
LLM_PROVIDER=openrouter
LLM_MODEL=xiaomi/mimo-v2-flash
LLM_API_KEY=your-openrouter-api-key
LLM_BASE_URL=https://openrouter.ai/api/v1

# Embedding model
EMBEDDING_PROVIDER=openrouter
EMBEDDING_MODEL=qwen/qwen3-embedding-8b

# =============================================================================
# REQUIRED — Neo4j (knowledge graph)
# =============================================================================

NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=moats2026

# =============================================================================
# OPTIONAL
# =============================================================================

# Local LLM (Ollama / LM Studio)
OLLAMA_BASE_URL=http://localhost:11434

# Data paths (defaults to ./data)
DATA_DIR=/data
SQLITE_PATH=
CHROMADB_PATH=

# Server
API_HOST=0.0.0.0
API_PORT=8000
FRONTEND_URL=http://localhost:3000

# Legacy (from previous project)
OPENROUTER_API_KEY=
